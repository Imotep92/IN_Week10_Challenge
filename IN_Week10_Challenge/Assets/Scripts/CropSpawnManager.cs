using UnityEngine;

public class CropSpawnManager : MonoBehaviour
{

    public GameObject _tomatoCropPrefab, _tomatoPrefab; //spawn gameobject 
    public Vector3 spawnOffset; //offset of spawn in vector3 off of harvester unit



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    void InstantiateCrop()
    {
        //spawn a crop prefab 
    }

    void instantiateVegetable()
    {
        //destroy crop and replace with tomato after set time
    }
}
